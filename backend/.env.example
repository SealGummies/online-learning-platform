# Environment variables template for Online Learning Platform
# Copy this file to .env and update with your actual values

# Application Environment
NODE_ENV=development
PORT=3761

# Database Configuration
# Choose ONE of the following database options:

# Option 1: Local MongoDB (uncomment to use local database)
# MONGODB_URI=mongodb://localhost:27017/online-learning-platform

# Option 2: Cloud MongoDB Atlas (replace with your actual credentials)
# Get your connection string from MongoDB Atlas dashboard
# Format: mongodb+srv://username:password@cluster.xxxxx.mongodb.net/database?retryWrites=true&w=majority&appName=AppName
MONGODB_URI=mongodb+srv://your-username:your-password@your-cluster.xxxxx.mongodb.net/online-learning-platform?retryWrites=true&w=majority&appName=YourAppName

# JWT Configuration
# Use a strong, unique secret key for production
JWT_SECRET=your-super-secure-jwt-secret-key-change-in-production
JWT_EXPIRE=30d

# Optional: Email Configuration (for future features)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USERNAME=your-email@gmail.com
# EMAIL_PASSWORD=your-app-password

# Optional: File Upload Configuration (for future features)
# MAX_FILE_SIZE=5242880
# UPLOAD_PATH=./uploads

# Optional: Redis Configuration (for caching, future features)
# REDIS_URL=redis://localhost:6379

# Optional: Cloudinary Configuration (for image uploads, future features)
# CLOUDINARY_CLOUD_NAME=your-cloud-name
# CLOUDINARY_API_KEY=your-api-key
# CLOUDINARY_API_SECRET=your-api-secret

# Optional: API Rate Limiting (requests per minute)
# RATE_LIMIT_REQUESTS=100
# RATE_LIMIT_WINDOW=15

# Optional: CORS Configuration
# CORS_ORIGIN=http://localhost:3000
# CORS_CREDENTIALS=true
